export type catFact = string;

export type Todo = {
  id: string;
  title: string;
  contents: string;
  isDone: boolean;
};

export type NewTodo = {
  id: string;
  title: string;
  contents: string;
  isDone: boolean;
};
